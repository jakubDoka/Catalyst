file "stages/parsing/src/state_gen.rs" {
    use "crate";

    struct AstDisplay {
        Ast
        AstData
        str
    }
}

file "stages/packaging/src/state_gen.rs" {
    use "packaging_t"
    use "diags"
    use "storage"

    struct PackageLoader {
        mut PackagingContext
        mut Workspace
        mut Interner
    }
}